{
  "name": "queuecure-v10-reception-control-stable",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "9.1.2"
  }
}